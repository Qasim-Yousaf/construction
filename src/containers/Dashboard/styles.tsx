import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    borderRadius: 8,
    padding: 16,
    height: 100,
  },
  text: {
    fontSize: 20,
  },
});
export default styles;
